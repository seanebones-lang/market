# G0 Evidence: Live Lock and Clean Baseline

- **Date:** 2026-08-16
- **Gate:** G0
- **Decision:** PASS
- **Authorization created by this decision:** G1 data-contract work only
- **Live-money authorization:** None

## Evidence

| Check | Result |
|---|---|
| Default offline tests | 69 passed, 1 network integration test deselected |
| Live-order build lock | Passed all runtime flag/mode/allow combinations; transport called 0 times |
| CLI live refusal | Passed with `MARKET_RH_LIVE` absent and set to `1` |
| Ruff lint | Passed |
| Ruff format check | Passed |
| Mypy | Passed, 27 source files |
| Safety-critical coverage | 85% (80% minimum) |
| Dependency audit | No known vulnerabilities |
| Default test network access | None |

## Review notes

- The obsolete duplicate `src/market/app/loop 2.py` was first preserved in Git history and then
  removed from the active package.
- Official Robinhood API terminology and action-scoped credential design replace the old
  username/password/TOTP concept.
- The current Robinhood boundary remains a non-network skeleton. Signed read-only API work belongs
  to G5.
- Existing saved backtests are labeled exploratory and cannot promote a strategy.
- G0 does not validate the backtester, paper runtime, persistence, execution, or strategy edge.
  Those later gates remain closed.

## CTO gate decision

G0 passes because the default suite is deterministic, unsafe configuration now fails startup, the
repository has an enforceable build-level live lock, and the quality gates are reproducible in CI.
Proceed to G1 only. Do not create a Robinhood credential or change the live-order build lock.
