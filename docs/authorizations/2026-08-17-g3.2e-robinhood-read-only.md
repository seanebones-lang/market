# G3.2e Robinhood Read-Only Measurement Authorization

- **Authorization ID:** `AUTH-G3.2E-RH-READONLY-2026-08-17`
- **Recorded at:** `2026-08-17T23:56:39Z`
- **Source:** Explicit account-owner authorization in the controlling Codex task
- **Personal identifiers recorded:** None
- **Capital authorization:** None
- **Order authorization:** None

## Authorized purpose

The account owner explicitly authorized the project to create and use one Robinhood Crypto
Trading API credential for the prospective G3.2d/G3.2e BTC-USD execution-cost study. This is a
narrow exception that pulls only the minimum read-only venue-measurement work forward while the
broader G3 and G5 gates remain open or locked.

The authorized API actions are limited to:

- Read crypto accounts;
- Read crypto products; and
- Read crypto quotes.

The authorized implementation and operation are limited to:

- local Ed25519 API-key-pair generation;
- storage of the private signing key and issued API key in macOS Keychain;
- deterministic request signing;
- authenticated `GET` requests to the four G3.2c allowlisted Robinhood v2 resources;
- one sanitized read-only preflight;
- a dated run plan bound to the frozen `rh-v2-cost-sampling-v1` protocol; and
- the protocol's 30-complete-UTC-day, 15-minute, BTC-USD cost-observation run.

## Explicit exclusions

This authorization does not permit:

- placing, modifying, canceling, or retrying an order;
- enabling either Robinhood API order action;
- transfers, withdrawals, deposits, or holdings changes;
- live trading or capital deployment;
- access to bank credentials, Robinhood login credentials, passwords, cookies, two-factor codes,
  wallet keys, or recovery phrases;
- persistence of account number, buying power, API key, private key, request signature, or
  authorization headers;
- holdings or order-history reads for this study; or
- treating quotes as fills, realized slippage, strategy evidence, or production readiness.

## Security and stopping conditions

- Secrets must not enter the repository, `.env`, a command-line argument, shell history, logs,
  evidence, fixtures, or chat.
- The private signing key and issued API key must be retrieved from macOS Keychain only at runtime.
- The HTTP client must be `GET`-only and must fail closed outside the exact endpoint and query-key
  allowlist.
- The existing compile-time order-submission lock remains false and must be regression-tested.
- A credential with any order permission is outside this authorization and must not be used.
- The preflight must stop on authentication, permission, schema, tradability, clock, redaction, or
  safety failure.
- Production collection must not begin until its dated run plan cites this authorization ID and is
  frozen before the first scheduled slot.

This record contains no account number or credential material.
